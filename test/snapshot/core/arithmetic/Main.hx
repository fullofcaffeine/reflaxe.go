class Main {
	static function main() {
		var a:Int = 7 + 5 * 2;
		var b:Float = 8.0 / 4.0 + 0.5;
		var input:Int = 7;
		var negated = -input;
		var complemented = ~input;
		Sys.println(a);
		Sys.println(b);
		Sys.println(negated);
		Sys.println(complemented);
	}
}
