class Main {
  static function main() {
    var left:String = "go";
    var right:String = "pher";
    var joined = left + right;
    var eq = left == right;
    var neq = left != right;
    Sys.println(joined);
    Sys.println(eq);
    Sys.println(neq);
  }
}
