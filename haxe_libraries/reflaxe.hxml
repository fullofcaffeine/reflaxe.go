# Vendored Reflaxe framework for source-checkout builds.
#
# Keeping this as a separate local library makes the dependency available on
# the initial classpath for Go and non-Go macro invocations alike.

-cp ${SCOPE_DIR}/vendor/reflaxe/src/

-D reflaxe=4.0.0-beta
